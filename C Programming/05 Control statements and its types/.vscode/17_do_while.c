//Do While Loop
#include <stdio.h>
int main()
{
    // do
    {
        /* code */
     }// while (condition);
     /*
     The do-while loop is an exit-controlled loop, which means it executes at least once, regardless of whether the condition is true or false. The loop body runs first, and then the condition is checked.
     */
    // !do While loop mainly uses for gaming purpose
    
    return 0;
}